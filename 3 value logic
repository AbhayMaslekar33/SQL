/*
* DB: Store
* Table: customers
* Question: adjust the following query to display the null values as "No Address"
*/

SELECT address2 
FROM customers

ans:
SELECT COALESCE(address2, 'No Address') AS address_2 FROM customers
